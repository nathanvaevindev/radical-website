import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Community",
  description:
    "The Radical Community — a curated network of AI professionals who learn together, make each other better, and build the foundation for ethical AI in Europe.",
};

export default function CommunityPage() {
  return <main />;
}
